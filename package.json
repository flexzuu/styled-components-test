{
  "name": "styled-components-test",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "jasmine-reporters": "^2.2.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "styled-components": "^1.0.10",
    "uigradients": "^0.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "report": "react-scripts test --env=jsdom --coverage --setupTestFrameworkScriptFile=reporter.js",
    "eject": "react-scripts eject"
  }
}
